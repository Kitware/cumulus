---
ec2_launch_instance_timeout: 1800 # 1/2 hour

# Very insecure!
ec2_pod_rules:
  - proto: all
    from_port: all
    to_port: all
    cidr_ip: 0.0.0.0/0

ec2_pod_rules_egress:
  - proto: all
    from_port: all
    to_port: all
    cidr_ip: 0.0.0.0/0
